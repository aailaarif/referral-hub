<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>

    <!-- Run Node via iisnode -->
    <handlers>
      <add name="iisnode" path="run.cjs" verb="*" modules="iisnode" />
    </handlers>

    <!-- Send every request to Node -->
    <rewrite>
      <rules>
        <rule name="NodeRoute" stopProcessing="true">
          <match url=".*" />
          <action type="Rewrite" url="run.cjs" />
        </rule>
      </rules>
    </rewrite>

    <!-- IMPORTANT: write logs to a writable folder -->
    <iisnode
      loggingEnabled="true"
      devErrorsEnabled="true"
      logDirectory="D:\home\LogFiles\iisnode"
    />

    <!-- Let Node responses pass through -->
    <httpErrors existingResponse="PassThrough" />

  </system.webServer>
</configuration>